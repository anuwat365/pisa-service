/**
 * Supported question types for templates.
 */
export type QuestionTypes = "multiple_choice" | "short_answer" | "essay_writing" | "mathematical_work";

/**
 * Supported accuracy types for scanning.
 */
export type AccuracyTypes = "perfect" | "partial" | "critical";